
import React from 'react';

interface TeamPhotoSectionProps {
  teamName: string;
}

const TeamPhotoSection: React.FC<TeamPhotoSectionProps> = ({ teamName }) => {
  // Component now empty as requested to remove the photo
  return null;
};

export default TeamPhotoSection;
